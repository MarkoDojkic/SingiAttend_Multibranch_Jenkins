spring:
  profiles:
    default: dev
  config:
    activate:
      on-profile: dev, kubernetes
  application:
    name: ${APP_NAME:SingiAttend-Server}
  main:
    allow-circular-references: true
  cloud:
    compatibility-verifier:
      enabled: false #Spring Boot 4.1.x not yet set as compatible with spring cloud 2025.1.1
  threads:
    virtual:
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
      cors:
        allowed-origins: "*"
  endpoint:
    health:
      enabled: true
      probes:
        enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
      version: ${project.version}

server:
  servlet:
    context-path: ${APP_CONTEXT_PATH:}
    session:
      cookie:
        path: / # Required when context-path is different in case of rewrites to keep SAML session cookie same
  port: ${PORT:62812}
  http2:
    enabled: true
  ssl:
    trust-store: classpath:truststore.jks
    trust-store-password: ${SSL_TRUST_STORE_PASSWORD}
    trust-store-type: JKS
    key-store: classpath:ssl_keystore-signed.p12
    key-store-type: PKCS12
    key-store-password: ${SSL_KEY_STORE_PASSWORD}
    key-password: ${SSL_KEY_PASSWORD}
    enabled: true
    enabled-protocols: TLSv1.2,TLSv1.3
    protocol: TLS

tenants: ${TENANTS}
mongo-host: ${MONGO_HOST:localhost:27017}
allowed-origins: ${ALLOWED_ORIGINS}
realm-name: ${REALM_NAME:singiattend-server}

iam:
  saml:
    relayingParties: ${IAM_SAML_RELAYING_PARTIES}
  ssl:
    truststore:
      path: classpath:saml/iam-truststore.p12
      password: ${IAM_SSL_TRUSTSTORE_PASSWORD}
      type: PKCS12
---
spring:
  config:
    activate:
      on-cloud-platform: kubernetes
  cloud:
    kubernetes:
      discovery:
        enabled: true
      client:
        namespace: ${KUBERNETES_NAMESPACE:default}
---
spring:
  config:
    activate:
      on-profile: dev
  cloud:
    kubernetes:
      discovery:
        enabled: false